[package]
name = "shader-converter-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1.7"
naga = { version = "0.19", features = ["glsl-in", "hlsl-out", "wgsl-out", "msl-out", "spv-out"] } 
